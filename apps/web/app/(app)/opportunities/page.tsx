import { OpportunitiesClient } from './OpportunitiesClient';

/** Opportunities room — contract-validated browse, filters, and grounded match summaries. */
export default function OpportunitiesPage(): JSX.Element {
  return <OpportunitiesClient />;
}
