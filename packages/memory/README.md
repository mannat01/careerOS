# @careeros/memory

MemoryService (profile/episodic/semantic/working) + graph/ (GraphMemoryService). Only package that touches memory tables.

See `docs/project-structure.md` for import boundaries and `docs/architecture.md` for role.
