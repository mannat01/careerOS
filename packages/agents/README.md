# @careeros/agents

Orchestrator + skill-agents + tool registry + plugin contract. Never imports db directly.

See `docs/project-structure.md` for import boundaries and `docs/architecture.md` for role.
