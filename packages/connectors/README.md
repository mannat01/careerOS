# @careeros/connectors

SourceConnector interface + sanctioned-source allow-list + adapters. Only package that talks to external sources.

See `docs/project-structure.md` for import boundaries and `docs/architecture.md` for role.
