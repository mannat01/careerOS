# @careeros/contracts

Shared zod schemas + inferred TS types (API DTOs, tool I/O, WS events). Single source of truth for client+server.

See `docs/project-structure.md` for import boundaries and `docs/architecture.md` for role.
